{
  "name": "@amdb/web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "postinstall": "playwright install",
    "dev": "vite --host 127.0.0.1 --port 5174",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run src",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@amdb/content": "workspace:*",
    "@amdb/core": "workspace:*"
  }
}
